cmake_minimum_required(VERSION 2.8)
project(Sobel)
find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable(SobelFilter.out SobelFilter.cpp )
target_link_libraries(SobelFilter.out ${OpenCV_LIBS})
