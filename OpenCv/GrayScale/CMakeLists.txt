cmake_minimum_required(VERSION 2.8)
project( OpenCvTry )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( grayScale.out grayScale.cpp )
target_link_libraries( grayScale.out ${OpenCV_LIBS} )
